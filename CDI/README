Module requirements: bliss, numpy, configobj
pip install bliss
pip install numpy
pip install configobj

Installation:
python setup.py install

Distribution:
python setup.py sdist
async_re-<version>.tar.gz will be in dist/

Installation from distribution:
pip install async_re-<version>.tar.gz

"Date" test:
To run, `python date_async_re.py command.inp`

See examples in examples/*
